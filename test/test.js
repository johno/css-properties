var assert = require('assert');
var cssProperties = require('..');

describe('css-properties', function() {

  it('should do something awesome', function() {
    assert.equal(cssProperties(), true);
  });
});
